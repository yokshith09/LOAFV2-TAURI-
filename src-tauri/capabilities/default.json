{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What Loaf's three windows are allowed to ask the core for. Commands defined by this app are always reachable; everything listed here is a Tauri core API, and each one is named individually rather than pulling in a default set.",
  "windows": ["companion", "dashboard", "bubble"],
  "permissions": [
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:event:allow-unlisten",

    "core:window:allow-outer-position",
    "core:window:allow-outer-size",
    "core:window:allow-set-position",
    "core:window:allow-available-monitors",
    "core:window:allow-current-monitor",
    "core:window:allow-scale-factor"
  ]
}
